name: 'neso_medallion'
version: '1.0.0'
config-version: 2

profile: 'neso_local'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  neso_medallion:
    bronze:
      +materialized: view
    silver:
      +materialized: incremental
    gold:
      +materialized: table
